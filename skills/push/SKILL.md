---
name: push
description: 为当前项目执行 git add、commit、push。触发词：push、推送、提交推送。
---

# push

为当前项目执行 git add、commit、push。

## 操作步骤

1. 执行 `git add .`
2. 执行 `git commit`（使用默认 commit message 或用户提供）
3. 执行 `git push`

## 使用场景

- 项目有未提交的更改时，执行 `/push` 一键推送
